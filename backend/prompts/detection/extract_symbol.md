The caller expects a {{asset_type}} symbol. If the text is clearly about a
different asset class, return that asset's correct symbol anyway rather than
forcing a wrong exchange.

TEXT:
"{{text}}"

Symbol:
