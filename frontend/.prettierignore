.next
node_modules
out
build
dist
package-lock.json
public
*.lock

# Generated by scripts/build_repo_facts.py; reflowing it breaks that gate.
lib/generated

# Playwright output. Gitignored, so CI never sees it — but a local run leaves
# it behind and `format:check` would then disagree with the CI gate.
test-results
playwright-report
